---
description: Run the project initialisation pipeline (grilling → to-spec → to-tickets) to turn a raw idea into planned tickets.
agent: init
---

Run the full project initialisation pipeline. Start with grilling to stress-test the idea, synthesise a spec, then break it into tracer-bullet tickets with blocking edges. After this command completes, the planner agent can pick up the tickets from `toplan`.
